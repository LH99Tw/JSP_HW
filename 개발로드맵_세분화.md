# 사케 커뮤니티&마켓 개발 로드맵 (세분화 버전)

## 📋 개발 원칙
1. **의존성 순서**: 하위 레이어(DB → DAO → Service → Servlet → JSP) 순서로 개발
2. **점진적 통합**: 각 기능을 독립적으로 완성 후 통합
3. **평가 요소 반영**: 각 단계마다 JSP 문법 요소를 명시적으로 활용
4. **테스트 가능 단위**: 각 단계 완료 후 즉시 테스트 가능하도록 구성

---

## 🎯 Phase 0: 프로젝트 셋업 & MVP 범위 확정

### 0.1 프로젝트 초기 설정
- [ ] Maven/Gradle 프로젝트 구조 생성
- [ ] `pom.xml` 또는 `build.gradle` 설정 (JSP/Servlet, JSTL, PostgreSQL 드라이버)
- [ ] `web.xml` 기본 설정 (인코딩, 세션 타임아웃)
- [ ] Docker Compose 기본 구조 (`docker-compose.yml`, `Dockerfile.tomcat`)
- [ ] `.gitignore` 설정

### 0.2 MVP 범위 최종 확정
**필수 기능 (MVP)**:
- 회원가입/로그인/로그아웃 (세션·쿠키)
- 사케 목록 + 상세 페이지
- 리뷰 작성/수정/삭제 + 사진 업로드
- 관리자 사케/상품 CRUD
- 장바구니 + 주문 요청
- 다국어(ko/en/ja) 기본 적용

**선택 기능 (시간 여유 시)**:
- ML 추천/리뷰 텍스트 분석
- 대시보드/통계
- 주문 상태 관리

---

## 🗄️ Phase 1: 데이터베이스 설계 & 인프라 구축

### 1.1 DB 스키마 설계 및 작성
**목표**: 모든 테이블 구조를 먼저 확정

- [ ] `init.sql` 작성
  - `users` (user_id, username, email, password, full_name, role, language, created_at)
  - `sake` (sake_id, name_ja, name_en, name_ko, brand, brewery, region_prefecture, style, alcohol_percent, volume_ml, polishing_ratio, nihonshu_do, acidity, sweetness_level, aroma_type, body_level, thumbnail_path, created_at)
  - `sake_review` (review_id, sake_id, user_id, rating, title, content, sweetness_score, dryness_score, acidity_score, aroma_score, body_score, ml_tags, created_at, updated_at)
  - `sake_product` (product_id, sake_id, price, stock, is_published, label, created_at)
  - `cart_item` (cart_item_id, user_id, product_id, quantity, created_at)
  - `orders` (order_id, user_id, total_amount, status, receiver_name, address, created_at)
  - `order_item` (order_item_id, order_id, product_id, quantity, price)
  - `uploads` (upload_id, user_id, original_filename, stored_filename, file_path, file_size, content_type, uploaded_at) - 리뷰 사진용
- [ ] 외래키 제약조건 설정
- [ ] 인덱스 추가 (성능 최적화)
- [ ] 초기 테스트 데이터 INSERT (관리자 계정, 샘플 사케 5~10개)

### 1.2 데이터베이스 연결 인프라
- [ ] `DataSource` 유틸리티 클래스 구현 (`com.example.util.DataSource`)
  - Connection Pool 설정 (HikariCP 또는 Tomcat JDBC Pool)
  - `getConnection()`, `closeConnection()` 메서드
- [ ] `web.xml`에 DataSource 리소스 설정 (선택사항)
- [ ] 연결 테스트 서블릿 작성 (`/test/db` - 개발용)

### 1.3 공통 유틸리티 클래스
- [ ] `DateUtil`: 날짜 포맷팅 유틸
- [ ] `JSONUtil`: JSON 파싱/생성 (ML 서비스 연동용)
- [ ] `LanguageUtil`: 언어 감지 및 Locale 설정
- [ ] `PasswordUtil`: 비밀번호 해싱 (BCrypt 또는 간단한 해시)

---

## 🔧 Phase 2: DAO 레이어 구현 (데이터 접근 계층)

**목표**: 화면 없이도 데이터 CRUD가 동작하는지 확인

### 2.1 UserDAO 구현
- [ ] `UserDAO` 클래스 생성 (`com.example.dao.UserDAO`)
- [ ] `insert(User user)`: 회원가입
- [ ] `findByUsername(String username)`: 로그인 검증
- [ ] `findByEmail(String email)`: 이메일 중복 체크
- [ ] `update(User user)`: 프로필 수정
- [ ] `findById(int userId)`: 사용자 조회
- [ ] **테스트**: 간단한 메인 메서드 또는 테스트 서블릿으로 INSERT/SELECT 확인

### 2.2 SakeDAO 구현
- [ ] `SakeDAO` 클래스 생성
- [ ] `findAll()`: 전체 목록
- [ ] `findById(int sakeId)`: 상세 조회
- [ ] `findByRegion(String region)`: 지역별 필터
- [ ] `findByStyle(String style)`: 스타일별 필터
- [ ] `insert(Sake sake)`: 사케 등록
- [ ] `update(Sake sake)`: 사케 수정
- [ ] `delete(int sakeId)`: 사케 삭제
- [ ] `search(String keyword)`: 검색 (이름, 브랜드)
- [ ] **테스트**: 샘플 데이터로 CRUD 동작 확인

### 2.3 ReviewDAO 구현
- [ ] `ReviewDAO` 클래스 생성
- [ ] `findBySakeId(int sakeId)`: 사케별 리뷰 목록
- [ ] `findByUserId(int userId)`: 사용자별 리뷰 목록
- [ ] `findById(int reviewId)`: 리뷰 상세
- [ ] `insert(Review review)`: 리뷰 작성
- [ ] `update(Review review)`: 리뷰 수정
- [ ] `delete(int reviewId)`: 리뷰 삭제
- [ ] `getAverageRating(int sakeId)`: 평균 평점 계산
- [ ] `getReviewCount(int sakeId)`: 리뷰 수 계산
- [ ] **테스트**: 리뷰 CRUD 및 통계 쿼리 확인

### 2.4 ProductDAO 구현
- [ ] `ProductDAO` 클래스 생성
- [ ] `findAll()`: 전체 상품 목록
- [ ] `findById(int productId)`: 상품 상세
- [ ] `findBySakeId(int sakeId)`: 사케별 상품 조회
- [ ] `insert(Product product)`: 상품 등록
- [ ] `update(Product product)`: 상품 수정
- [ ] `delete(int productId)`: 상품 삭제
- [ ] `updateStock(int productId, int quantity)`: 재고 업데이트

### 2.5 CartDAO 구현
- [ ] `CartDAO` 클래스 생성
- [ ] `findByUserId(int userId)`: 사용자 장바구니 목록
- [ ] `insert(CartItem item)`: 장바구니 추가
- [ ] `updateQuantity(int cartItemId, int quantity)`: 수량 변경
- [ ] `delete(int cartItemId)`: 장바구니 삭제
- [ ] `clearCart(int userId)`: 장바구니 비우기 (주문 후)

### 2.6 OrderDAO 구현
- [ ] `OrderDAO` 클래스 생성
- [ ] `insert(Order order)`: 주문 생성
- [ ] `insertOrderItem(OrderItem item)`: 주문 상품 추가
- [ ] `findByUserId(int userId)`: 사용자 주문 목록
- [ ] `findById(int orderId)`: 주문 상세

---

## 🛡️ Phase 3: 공통 인프라 & 인증 시스템

### 3.1 필터(Filter) 구현
- [ ] `CharacterEncodingFilter`: UTF-8 인코딩 설정
  - `web.xml`에 필터 매핑
- [ ] `AuthFilter`: 로그인 체크 필터
  - `/mypage/*`, `/review/*`, `/cart/*`, `/order/*` 패턴에 적용
  - 세션에 `user` 없으면 `/login?redirect=원래URL`로 리다이렉트
- [ ] `AdminAuthFilter`: 관리자 권한 체크
  - `/admin/*` 패턴에 적용
  - `role == "ADMIN"` 확인
- [ ] `LanguageFilter`: 언어 설정 필터 (선택사항)
  - 쿠키/세션에서 언어 읽어서 `Locale` 설정

### 3.2 세션 & 쿠키 관리 유틸
- [ ] 세션 헬퍼 메서드 (선택사항)
  - `SessionUtil.getUser(HttpSession session)`
  - `SessionUtil.isLoggedIn(HttpSession session)`
  - `SessionUtil.isAdmin(HttpSession session)`

### 3.3 공통 JSP 레이아웃
- [ ] `jsp/common/header.jspf` 생성
  - **평가 요소**: `<%@ include %>` 디렉티브 사용
  - 로그인 여부에 따른 메뉴 분기 (JSTL `<c:if>` 사용)
  - 언어 선택 드롭다운
- [ ] `jsp/common/footer.jspf` 생성
- [ ] `jsp/common/nav.jspf` 생성 (선택사항)
- [ ] 기본 CSS 파일 (`css/style.css`) 생성 또는 Bootstrap/Bulma CDN 추가

---

## 👤 Phase 4: 회원 기능 (인증/인가)

### 4.1 회원가입
- [ ] `register.jsp` 작성
  - **평가 요소**: 
    - 폼 태그 (`<form>`, `<input>`, `<select>`)
    - 클라이언트 측 유효성 검사 (JavaScript)
    - JSTL/EL로 에러 메시지 표시
- [ ] `RegisterServlet` 구현
  - POST 요청 처리
  - **서버 측 유효성 검사**: 이메일 형식, 비밀번호 길이, 중복 체크
  - 비밀번호 해싱 후 `UserDAO.insert()` 호출
  - 성공 시 `/login`으로 리다이렉트
- [ ] **테스트**: 회원가입 플로우 확인

### 4.2 로그인
- [ ] `login.jsp` 작성
  - 아이디/비밀번호 입력 폼
  - "아이디 저장" 체크박스
  - `redirect` 파라미터 처리 (로그인 후 원래 페이지로)
- [ ] `LoginServlet` 구현
  - POST 요청 처리
  - `UserDAO.findByUsername()`으로 사용자 조회
  - 비밀번호 검증
  - 세션에 `user`, `userId`, `username`, `role` 저장
  - "아이디 저장" 체크 시 쿠키 발급 (`Cookie` 객체 사용)
  - 성공 시 `redirect` 파라미터 또는 `/`로 리다이렉트
- [ ] **테스트**: 로그인/세션 저장 확인

### 4.3 로그아웃
- [ ] `LogoutServlet` 구현
  - `session.invalidate()` 호출
  - 쿠키 삭제 (선택사항)
  - `/`로 리다이렉트
- [ ] **테스트**: 로그아웃 후 세션 무효화 확인

### 4.4 마이페이지 (기본)
- [ ] `mypage/index.jsp` 작성
  - **평가 요소**: 
    - 세션에서 사용자 정보 가져오기 (`${sessionScope.user}`)
    - JSTL `<c:forEach>`로 내 리뷰 목록 표시
  - 사용자 정보 표시
  - 내 리뷰 목록 (간단 버전)
- [ ] `MypageServlet` 구현 (선택사항, JSP에서 직접 DAO 호출도 가능)

---

## 🍶 Phase 5: 커뮤니티 코어 - 사케 정보 & 리뷰

### 5.1 사케 목록 페이지
- [ ] `SakeListServlet` 구현
  - GET `/sake/list`
  - 필터 파라미터 처리 (region, style)
  - 페이징 파라미터 처리 (page, size)
  - `SakeDAO.findAll()` 또는 필터 메서드 호출
  - 결과를 `request.setAttribute()`로 전달
- [ ] `jsp/sake/list.jsp` 작성
  - **평가 요소**:
    - JSTL `<c:forEach>`로 사케 목록 반복
    - EL `${sake.name}` 표현식 사용
    - `<jsp:include>`로 공통 헤더/푸터 포함
  - 카드 형태로 사케 목록 표시
  - 필터 UI (드롭다운)
  - 페이징 UI

### 5.2 사케 상세 페이지
- [ ] `SakeDetailServlet` 구현
  - GET `/sake/detail?sakeId=`
  - `SakeDAO.findById()` 호출
  - `ReviewDAO.getAverageRating()`, `getReviewCount()` 호출
  - 해당 사케의 리뷰 목록 조회 (`ReviewDAO.findBySakeId()`)
  - `request.setAttribute()`로 전달
- [ ] `jsp/sake/detail.jsp` 작성
  - **평가 요소**:
    - `<jsp:useBean>`으로 사케 객체 바인딩 (선택사항)
    - 스크립틀릿으로 평점 계산 (`<% %>`)
    - JSTL로 리뷰 목록 표시
  - 상단: 사케 기본 정보 + 평균 평점 + 리뷰 수
  - 중간: 맛 프로파일 시각화
  - 하단: 리뷰 리스트 (페이징)

### 5.3 리뷰 작성
- [ ] `ReviewFormServlet` 구현
  - GET `/review/form?sakeId=`: 리뷰 작성 폼 표시
  - 로그인 체크 (AuthFilter로 처리)
- [ ] `jsp/review/form.jsp` 작성
  - **평가 요소**:
    - 폼 태그 (`<form enctype="multipart/form-data">`)
    - 파일 업로드 `<input type="file">`
    - 클라이언트 측 유효성 검사 (별점 필수, 텍스트 최소 글자수)
  - 별점 선택 (1~5)
  - 맛 척도 입력 (단맛, 산미, 향, 바디감)
  - 리뷰 텍스트 입력
  - 사진 업로드
- [ ] `ReviewCreateServlet` 구현
  - POST `/review/create`
  - **서버 측 유효성 검사**: 별점 필수, 텍스트 길이
  - **파일 업로드 처리**: 
    - `Part` API 또는 Apache Commons FileUpload 사용
    - `/uploads/review/` 디렉토리에 저장
    - 파일명 UUID로 변환 (보안)
    - `uploads` 테이블에 파일 정보 저장
  - `ReviewDAO.insert()` 호출
  - 성공 시 `/sake/detail?sakeId=`로 리다이렉트

### 5.4 리뷰 수정
- [ ] `ReviewUpdateServlet` 구현
  - GET `/review/edit?reviewId=`: 수정 폼 표시
  - POST `/review/update`: 수정 처리
  - 작성자 본인만 수정 가능 체크
- [ ] `jsp/review/form.jsp` 재사용 (수정 모드로)

### 5.5 리뷰 삭제
- [ ] `ReviewDeleteServlet` 구현
  - POST `/review/delete?reviewId=`
  - 작성자 본인만 삭제 가능 체크
  - `ReviewDAO.delete()` 호출
  - 관련 파일 삭제 (선택사항)

### 5.6 리뷰 사진 표시
- [ ] `FileViewServlet` 구현 (이미 있으면 재사용)
  - GET `/file/view?uploadId=`
  - 파일 다운로드/표시

---

## 🛒 Phase 6: 마켓 기능 - 상품 & 장바구니 & 주문

### 6.1 상품 목록
- [ ] `ProductListServlet` 구현
  - GET `/product/list`
  - 필터 파라미터 처리 (region, style, priceRange)
  - `ProductDAO.findAll()` 호출 (JOIN으로 사케 정보 포함)
- [ ] `jsp/product/list.jsp` 작성
  - **평가 요소**: JSTL로 상품 목록 반복, EL 표현식
  - 카드 형태로 상품 표시
  - 가격, 재고, 평점 표시
  - 필터 UI

### 6.2 상품 상세
- [ ] `ProductDetailServlet` 구현
  - GET `/product/detail?productId=`
  - `ProductDAO.findById()` 호출
  - 사케 정보도 함께 조회 (JOIN)
  - 리뷰 목록도 함께 조회 (5.2 재사용)
- [ ] `jsp/product/detail.jsp` 작성
  - 상품 정보 (가격, 재고)
  - "장바구니 담기" 버튼
  - 리뷰 탭 (사케 상세 페이지 재사용)

### 6.3 장바구니
- [ ] `CartAddServlet` 구현
  - POST `/cart/add`
  - 로그인 체크
  - `CartDAO.insert()` 호출
- [ ] `CartViewServlet` 구현
  - GET `/cart`
  - `CartDAO.findByUserId()` 호출
- [ ] `jsp/cart/list.jsp` 작성
  - **평가 요소**: JSTL로 장바구니 아이템 반복
  - 수량 변경 폼
  - 총 금액 계산 (스크립틀릿 또는 JSTL)
- [ ] `CartUpdateServlet` 구현
  - POST `/cart/update`
  - 수량 변경
- [ ] `CartDeleteServlet` 구현
  - POST `/cart/delete`
  - 장바구니 아이템 삭제

### 6.4 주문 요청
- [ ] `OrderFormServlet` 구현
  - GET `/order/form`
  - 장바구니 정보 조회
  - 배송 정보 입력 폼 표시
- [ ] `jsp/order/form.jsp` 작성
  - **평가 요소**: 폼 태그, 유효성 검사
  - 주문 상품 목록
  - 배송 정보 입력 (이름, 주소, 연락처)
  - 총 금액 표시
- [ ] `OrderCreateServlet` 구현
  - POST `/order/create`
  - **서버 측 유효성 검사**: 배송 정보 필수값 체크
  - 트랜잭션 처리:
    - `OrderDAO.insert()` 호출
    - `OrderDAO.insertOrderItem()` 반복 호출
    - `CartDAO.clearCart()` 호출
    - `ProductDAO.updateStock()` 호출 (재고 차감)
  - 성공 시 `/order/complete?orderId=`로 리다이렉트
- [ ] `jsp/order/complete.jsp` 작성
  - 주문 완료 메시지
  - 주문 번호 표시

---

## 👨‍💼 Phase 7: 관리자 페이지

### 7.1 관리자 레이아웃
- [ ] `jsp/admin/common/header.jsp` 생성
  - **평가 요소**: `<%@ include %>` 디렉티브
  - 관리자 전용 메뉴
- [ ] `jsp/admin/common/sidebar.jsp` 생성 (선택사항)

### 7.2 관리자 대시보드
- [ ] `AdminDashboardServlet` 구현
  - GET `/admin/dashboard`
  - 통계 데이터 조회:
    - 사케 수, 리뷰 수, 상품 수, 오늘 주문 수
    - 인기 사케 TOP 10
- [ ] `jsp/admin/dashboard.jsp` 작성
  - **평가 요소**: 스크립틀릿으로 통계 계산
  - 간단한 통계 카드 표시

### 7.3 사케 관리 (CRUD)
- [ ] `AdminSakeListServlet` 구현
  - GET `/admin/sake/list`
  - 검색/필터 기능
- [ ] `jsp/admin/sake/list.jsp` 작성
  - **평가 요소**: JSTL로 목록 반복, EL 표현식
  - 사케 목록 테이블
  - "등록", "수정", "삭제" 버튼
- [ ] `AdminSakeFormServlet` 구현
  - GET `/admin/sake/form`: 등록 폼
  - GET `/admin/sake/form?sakeId=`: 수정 폼
- [ ] `jsp/admin/sake/form.jsp` 작성
  - **평가 요소**: 
    - 폼 태그 (모든 입력 필드)
    - 파일 업로드 (대표 이미지)
    - 클라이언트/서버 측 유효성 검사
    - `<jsp:useBean>` 사용 (선택사항)
  - 사케 정보 입력 폼
- [ ] `AdminSakeCreateServlet` 구현
  - POST `/admin/sake/create`
  - 파일 업로드 처리
  - `SakeDAO.insert()` 호출
- [ ] `AdminSakeUpdateServlet` 구현
  - POST `/admin/sake/update`
  - `SakeDAO.update()` 호출
- [ ] `AdminSakeDeleteServlet` 구현
  - POST `/admin/sake/delete`
  - `SakeDAO.delete()` 호출

### 7.4 상품 관리 (CRUD)
- [ ] `AdminProductListServlet` 구현
- [ ] `jsp/admin/product/list.jsp` 작성
- [ ] `AdminProductFormServlet` 구현
- [ ] `jsp/admin/product/form.jsp` 작성
  - **평가 요소**: 폼 태그, 유효성 검사
  - 사케 선택 드롭다운
  - 가격, 재고, 노출 여부 입력
- [ ] `AdminProductCreateServlet` 구현
- [ ] `AdminProductUpdateServlet` 구현
- [ ] `AdminProductDeleteServlet` 구현

### 7.5 리뷰 관리
- [ ] `AdminReviewListServlet` 구현
  - GET `/admin/review/list`
  - 필터 (사케, 사용자, 평점)
- [ ] `jsp/admin/review/list.jsp` 작성
  - **평가 요소**: JSTL로 리뷰 목록 반복
  - 리뷰 목록 테이블
  - "삭제" 버튼
- [ ] `AdminReviewDeleteServlet` 구현
  - POST `/admin/review/delete`
  - 리뷰 삭제 또는 숨김 처리

### 7.6 주문 관리
- [ ] `AdminOrderListServlet` 구현
  - GET `/admin/order/list`
- [ ] `jsp/admin/order/list.jsp` 작성
  - **평가 요소**: JSTL로 주문 목록 반복
  - 주문 목록 테이블
  - 주문 상세 링크
- [ ] `AdminOrderDetailServlet` 구현
  - GET `/admin/order/detail?orderId=`
- [ ] `jsp/admin/order/detail.jsp` 작성
  - 주문 상세 정보
  - 주문 상품 목록

---

## 🌐 Phase 8: 다국어 지원 (i18n)

### 8.1 다국어 리소스 파일 작성
- [ ] `messages_ko.properties` 작성
  - 모든 화면의 텍스트 키-값 쌍 정의
- [ ] `messages_en.properties` 작성
- [ ] `messages_ja.properties` 작성 (선택사항)

### 8.2 언어 선택 기능
- [ ] `LanguageServlet` 구현
  - GET `/language/change?lang=ko|en|ja`
  - 쿠키 또는 세션에 언어 저장
  - 원래 페이지로 리다이렉트
- [ ] 헤더에 언어 선택 드롭다운 추가

### 8.3 JSP에 다국어 적용
- [ ] 모든 JSP 파일에 `<%@ taglib prefix="fmt" %>` 추가
- [ ] `<fmt:setBundle>` 및 `<fmt:message>` 태그 적용
- [ ] 주요 화면부터 순차적으로 적용:
  - [ ] `login.jsp`, `register.jsp`
  - [ ] `sake/list.jsp`, `sake/detail.jsp`
  - [ ] `review/form.jsp`
  - [ ] `product/list.jsp`, `product/detail.jsp`
  - [ ] `cart/list.jsp`
  - [ ] `admin/*.jsp`

### 8.4 언어별 테스트
- [ ] 각 언어로 전환하여 텍스트 표시 확인

---

## 🤖 Phase 9: ML 마이크로서비스 연동

### 9.1 FastAPI ML 서비스 기본 구조
- [ ] `ml-service/app/main.py` 작성
  - FastAPI 앱 초기화
  - `GET /health` 엔드포인트
- [ ] `ml-service/requirements.txt` 작성
- [ ] `ml-service/Dockerfile` 작성
- [ ] Docker Compose에 ML 서비스 추가

### 9.2 ML API 엔드포인트 구현 (더미 버전)
- [ ] `POST /api/v1/recommend` 구현
  - 입력: 사용자 ID, 선호 맛 정보
  - 출력: 추천 사케 리스트 (더미 데이터)
- [ ] `POST /api/v1/analyze-taste` 구현
  - 입력: 리뷰 텍스트
  - 출력: 맛 태그 리스트 (키워드 기반 더미 로직)

### 9.3 Java 클라이언트 구현
- [ ] `MLClient` 유틸리티 클래스 생성
  - `recommendSake()` 메서드 (Apache HttpClient 또는 HttpURLConnection)
  - `analyzeTaste()` 메서드
  - JSON 요청/응답 처리

### 9.4 추천 기능 통합
- [ ] `RecommendServlet` 구현
  - GET `/recommend`
  - 사용자 정보 기반으로 ML 서비스 호출
  - 추천 사케 목록 표시
- [ ] `jsp/recommend/list.jsp` 작성
  - 추천 사케 목록 표시

### 9.5 리뷰 텍스트 분석 통합
- [ ] `ReviewCreateServlet` 수정
  - 리뷰 저장 후 `MLClient.analyzeTaste()` 호출
  - 반환된 태그를 `ml_tags` 컬럼에 저장
- [ ] `jsp/sake/detail.jsp` 수정
  - ML 태그 표시 (선택사항)

### 9.6 ML 모델 고도화 (시간 여유 시)
- [ ] scikit-learn 모델 학습 (KNN, TF-IDF 등)
- [ ] 실제 추천 로직 구현
- [ ] 모델 성능 평가

---

## 🎨 Phase 10: UI/UX 다듬기 & 최종 통합

### 10.1 공통 레이아웃 정리
- [ ] `header.jspf` 최종 정리
  - 반응형 메뉴
  - 언어 선택 UI 개선
- [ ] `footer.jspf` 최종 정리
- [ ] 공통 CSS 정리 (`css/style.css`)
  - 일관된 색상, 폰트, 간격

### 10.2 스크립틀릿 → JSTL/EL 리팩토링
- [ ] 각 JSP 파일 검토
- [ ] 불필요한 스크립틀릿을 JSTL/EL로 교체
- [ ] **주의**: 평가 요소를 위해 일부 스크립틀릿은 유지 (명시적 사용)

### 10.3 에러 처리
- [ ] `web.xml`에 에러 페이지 설정
  - 404, 500 에러 페이지
- [ ] `jsp/error/404.jsp` 작성
- [ ] `jsp/error/500.jsp` 작성
- [ ] 예외 처리 필터 또는 서블릿 추가

### 10.4 최종 테스트
- [ ] 회원 기능 플로우 테스트
- [ ] 커뮤니티 기능 플로우 테스트
- [ ] 마켓 기능 플로우 테스트
- [ ] 관리자 기능 플로우 테스트
- [ ] 다국어 전환 테스트
- [ ] ML 서비스 연동 테스트
- [ ] 파일 업로드/다운로드 테스트

### 10.5 문서화
- [ ] `README.md` 작성
  - 프로젝트 개요
  - 실행 방법
  - 주요 기능 설명
- [ ] API 문서 정리 (선택사항)

---

## 📊 개발 우선순위 요약

### 🔴 최우선 (MVP 완성)
1. Phase 1: DB 설계 & 인프라
2. Phase 2: DAO 레이어
3. Phase 3: 공통 인프라 & 인증
4. Phase 4: 회원 기능
5. Phase 5: 커뮤니티 코어
6. Phase 6: 마켓 기능
7. Phase 7: 관리자 페이지

### 🟡 중요 (평가 요소 강화)
8. Phase 8: 다국어 지원

### 🟢 선택 (시간 여유 시)
9. Phase 9: ML 마이크로서비스 연동
10. Phase 10: UI/UX 다듬기

---

## 💡 개발 팁

1. **단계별 커밋**: 각 Phase 완료 시 커밋하여 롤백 가능하게
2. **테스트 우선**: DAO 레이어부터 테스트 가능하도록 구현
3. **평가 요소 체크리스트**: 각 JSP 파일 작성 시 평가 요소 사용 여부 확인
4. **코드 재사용**: 공통 컴포넌트(헤더, 푸터) 적극 활용
5. **보안**: SQL Injection 방지 (PreparedStatement), XSS 방지 (입력값 이스케이프)

