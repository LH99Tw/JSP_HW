# 개발 진행 과정

이 문서는 프로젝트 개발 진행 상황을 단계별로 기록합니다.

---

## 📅 2024년 개발 시작

### Phase 0: 프로젝트 셋업 & MVP 범위 확정 ✅

**완료 항목**:
- ✅ 프로젝트 구조 확인 (기존 구조 활용)
- ✅ `web.xml` 기본 설정 확인 (인코딩, 세션 타임아웃 설정됨)
- ✅ Docker Compose 기본 구조 확인 (`docker-compose.yml` 존재)
- ✅ `.gitignore` 설정 확인

**MVP 범위 확정**:
- 회원가입/로그인/로그아웃 (세션·쿠키)
- 사케 목록 + 상세 페이지
- 리뷰 작성/수정/삭제 + 사진 업로드
- 관리자 사케/상품 CRUD
- 장바구니 + 주문 요청
- 다국어(ko/en/ja) 기본 적용

---

### Phase 1: 데이터베이스 설계 & 인프라 구축 ✅

**완료 일시**: 2024년

#### 1.1 DB 스키마 설계 및 작성 ✅

**작업 내용**:
- `docker/init.sql` 파일에 사케 도메인 테이블 추가
- 기존 `users` 테이블에 `role` 컬럼 추가 (USER/ADMIN 구분)

**생성된 테이블**:
1. ✅ `users` - 사용자 테이블 (role 컬럼 추가됨)
2. ✅ `sake` - 사케 기본 정보 테이블
3. ✅ `sake_review` - 사케 리뷰 테이블
4. ✅ `sake_product` - 판매용 상품 테이블
5. ✅ `cart_item` - 장바구니 테이블
6. ✅ `orders` - 주문 테이블
7. ✅ `order_item` - 주문 상품 테이블
8. ✅ `uploads` - 파일 업로드 테이블 (기존)
9. ✅ `ml_results` - ML 결과 테이블 (기존)

**인덱스 생성**:
- 사용자 관련: `idx_users_username`, `idx_users_email`, `idx_users_role`
- 사케 도메인: `idx_sake_region`, `idx_sake_style`, `idx_sake_brand`
- 리뷰: `idx_sake_review_sake_id`, `idx_sake_review_user_id`
- 상품: `idx_sake_product_sake_id`
- 장바구니: `idx_cart_item_user_id`
- 주문: `idx_orders_user_id`, `idx_order_item_order_id`

**초기 데이터**:
- ✅ 샘플 사케 데이터 5개 삽입 (다사이, 구보타, 팔해산, 신정, 십사대)
- ✅ 관리자 계정 생성 (username: admin)

**테스트 결과**:
```sql
-- 테이블 목록 확인
✅ 9개 테이블 모두 생성 완료
- cart_item
- ml_results
- order_item
- orders
- sake
- sake_product
- sake_review
- uploads
- users

-- 샘플 데이터 확인
✅ 사케 데이터: 5개
✅ 관리자 계정: 생성 완료
```

#### 1.2 데이터베이스 연결 인프라 ✅

**작업 내용**:
- `com.example.util.DataSource` 유틸리티 클래스 생성
  - 환경 변수 기반 DB 연결 설정 (Docker 환경 지원)
  - `getConnection()`: DB 연결 가져오기
  - `closeConnection()`: 연결 안전하게 닫기
  - `testConnection()`: 연결 테스트 메서드

**파일 위치**: `src/main/java/com/example/util/DataSource.java`

**주요 기능**:
- 환경 변수에서 DB 설정 읽기 (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD)
- 기본값 설정 (로컬 개발 환경 지원)
- PostgreSQL JDBC 드라이버 자동 로드

#### 1.3 DB 연결 테스트 서블릿 생성 ✅

**작업 내용**:
- `com.example.servlet.DBTestServlet` 생성
- 개발용 DB 연결 테스트 페이지 제공
- 테이블 목록 및 DB 정보 확인 기능

**파일 위치**: `src/main/java/com/example/servlet/DBTestServlet.java`

**접근 경로**: `http://localhost:8080/test/db` (Tomcat 실행 후)

**테스트 결과**:
- ✅ PostgreSQL 컨테이너 실행 확인
- ✅ 모든 테이블 생성 확인
- ✅ 샘플 데이터 삽입 확인

---

### Phase 2: DAO 레이어 구현 ✅

**완료 일시**: 2024년

#### 2.1 Model 클래스 생성 ✅

**작업 내용**:
- 모든 도메인 모델 클래스 생성

**생성된 Model 클래스**:
1. ✅ `User.java` - 사용자 모델
2. ✅ `Sake.java` - 사케 모델
3. ✅ `SakeReview.java` - 사케 리뷰 모델
4. ✅ `SakeProduct.java` - 사케 상품 모델
5. ✅ `CartItem.java` - 장바구니 아이템 모델
6. ✅ `Order.java` - 주문 모델
7. ✅ `OrderItem.java` - 주문 상품 모델

**파일 위치**: `src/main/java/com/example/model/`

#### 2.2 DAO 클래스 구현 ✅

**작업 내용**:
- 모든 DAO 클래스 구현 및 CRUD 메서드 작성

**생성된 DAO 클래스**:
1. ✅ `UserDAO.java`
   - `insert()`: 회원가입
   - `findByUsername()`: 로그인 검증
   - `findByEmail()`: 이메일 중복 체크
   - `findById()`: 사용자 조회
   - `update()`: 프로필 수정
   - `updatePassword()`: 비밀번호 변경

2. ✅ `SakeDAO.java`
   - `findAll()`: 전체 목록
   - `findById()`: 상세 조회
   - `findByRegion()`: 지역별 필터
   - `findByStyle()`: 스타일별 필터
   - `search()`: 검색 (이름, 브랜드)
   - `insert()`: 사케 등록
   - `update()`: 사케 수정
   - `delete()`: 사케 삭제

3. ✅ `ReviewDAO.java`
   - `findBySakeId()`: 사케별 리뷰 목록
   - `findByUserId()`: 사용자별 리뷰 목록
   - `findById()`: 리뷰 상세
   - `insert()`: 리뷰 작성
   - `update()`: 리뷰 수정
   - `delete()`: 리뷰 삭제
   - `getAverageRating()`: 평균 평점 계산
   - `getReviewCount()`: 리뷰 수 계산

4. ✅ `ProductDAO.java`
   - `findAll()`: 전체 상품 목록
   - `findById()`: 상품 상세
   - `findBySakeId()`: 사케별 상품 조회
   - `insert()`: 상품 등록
   - `update()`: 상품 수정
   - `delete()`: 상품 삭제
   - `updateStock()`: 재고 업데이트

5. ✅ `CartDAO.java`
   - `findByUserId()`: 사용자 장바구니 목록
   - `insert()`: 장바구니 추가 (중복 시 수량 증가)
   - `updateQuantity()`: 수량 변경
   - `delete()`: 장바구니 삭제
   - `clearCart()`: 장바구니 비우기

6. ✅ `OrderDAO.java`
   - `insert()`: 주문 생성 (트랜잭션 처리)
   - `insertOrderItem()`: 주문 상품 추가
   - `findByUserId()`: 사용자 주문 목록
   - `findById()`: 주문 상세 (주문 상품 포함)

**파일 위치**: `src/main/java/com/example/dao/`

#### 2.3 DAO 테스트 서블릿 생성 ✅

**작업 내용**:
- `DAOTestServlet` 생성
- 각 DAO의 주요 메서드 테스트 기능 제공

**파일 위치**: `src/main/java/com/example/servlet/DAOTestServlet.java`

**접근 경로**: `http://localhost:8080/test/dao` (Tomcat 실행 후)

**테스트 항목**:
- SakeDAO: 전체 조회, 상세 조회
- ReviewDAO: 평균 평점, 리뷰 수 계산
- UserDAO: 관리자 계정 조회
- ProductDAO: 전체 상품 조회

---

---

### Phase 3: 공통 인프라 & 인증 시스템 ✅

**완료 일시**: 2024년

#### 3.1 필터(Filter) 구현 ✅

**작업 내용**:
- 모든 필터 클래스 구현 및 web.xml에 등록

**생성된 필터**:
1. ✅ `CharacterEncodingFilter` (기존 파일 활용)
   - UTF-8 인코딩 설정
   - web.xml에 필터 매핑 완료

2. ✅ `AuthFilter`
   - 로그인 체크 필터
   - `/mypage/*`, `/review/*`, `/cart/*`, `/order/*` 패턴에 적용
   - 세션에 `user` 없으면 `/login?redirect=원래URL`로 리다이렉트
   - 제외 경로 설정 (로그인, 회원가입, 테스트, 정적 리소스)

3. ✅ `AdminAuthFilter`
   - 관리자 권한 체크 필터
   - `/admin/*` 패턴에 적용
   - `role == "ADMIN"` 확인
   - 권한 없으면 로그인 페이지로 리다이렉트

**파일 위치**: `src/main/java/com/example/filter/`

#### 3.2 세션 & 쿠키 관리 유틸 ✅

**작업 내용**:
- `SessionUtil` 유틸리티 클래스 구현

**주요 메서드**:
- `getUser(HttpSession)`: 세션에서 사용자 객체 가져오기
- `setUser(HttpSession, User)`: 세션에 사용자 저장
- `isLoggedIn(HttpSession)`: 로그인 여부 확인
- `isAdmin(HttpSession)`: 관리자 여부 확인
- `getUserId(HttpSession)`: 사용자 ID 가져오기
- `getUsername(HttpSession)`: 사용자명 가져오기
- `getRole(HttpSession)`: 역할 가져오기
- `invalidate(HttpSession)`: 세션 무효화

**파일 위치**: `src/main/java/com/example/util/SessionUtil.java`

#### 3.3 공통 JSP 레이아웃 ✅

**작업 내용**:
- 공통 헤더/푸터 JSP 파일 생성 및 업데이트

**생성/수정된 파일**:
1. ✅ `jsp/common/header.jspf`
   - **평가 요소**: `<%@ include %>` 디렉티브 사용 (web.xml에서 자동 include)
   - JSTL `<c:if>`, `<c:choose>` 태그로 로그인 여부에 따른 메뉴 분기
   - 언어 선택 드롭다운 (한국어, 영어, 일본어)
   - Bootstrap 5 CDN 추가
   - Font Awesome 아이콘 추가
   - 반응형 네비게이션 바
   - 검색 기능 UI

2. ✅ `jsp/common/footer.jspf`
   - 푸터 레이아웃
   - 회사 정보, 고객지원, 법적 고지 링크

3. ✅ `css/style.css`
   - 기본 스타일 정의
   - 카드, 버튼, 뱃지 스타일
   - 반응형 디자인 지원
   - Bootstrap과 호환되는 커스텀 스타일

**주요 기능**:
- 로그인 여부에 따른 메뉴 표시
- 관리자 메뉴 표시 (관리자만)
- 언어 선택 기능
- 반응형 디자인

---

---

### Phase 4: 회원 기능 (인증/인가) ✅

**완료 일시**: 2024년

#### 4.1 회원가입 ✅

**작업 내용**:
- 회원가입 페이지 및 서블릿 구현

**생성된 파일**:
1. ✅ `RegisterServlet.java`
   - GET: 회원가입 페이지 표시
   - POST: 회원가입 처리
   - 서버 측 유효성 검사 (이메일 형식, 비밀번호 길이, 중복 체크)
   - 비밀번호 해싱 후 DB 저장
   - 성공 시 로그인 페이지로 리다이렉트

2. ✅ `register.jsp`
   - **평가 요소**: 
     - 폼 태그 (`<form>`, `<input>`, `<select>`)
     - 클라이언트 측 유효성 검사 (JavaScript)
     - JSTL/EL로 에러 메시지 표시 (`${error}`)
   - Bootstrap 5 스타일 적용
   - 비밀번호 확인 실시간 검증

**파일 위치**: 
- `src/main/java/com/example/servlet/RegisterServlet.java`
- `src/main/webapp/jsp/user/register.jsp`

#### 4.2 로그인 ✅

**작업 내용**:
- 로그인 페이지 및 서블릿 구현

**생성된 파일**:
1. ✅ `LoginServlet.java`
   - GET: 로그인 페이지 표시 (이미 로그인한 경우 홈으로 리다이렉트)
   - POST: 로그인 처리
   - `UserDAO.findByUsername()`으로 사용자 조회
   - 비밀번호 검증
   - 세션에 사용자 정보 저장 (`SessionUtil.setUser()`)
   - "아이디 저장" 체크 시 쿠키 발급 (`Cookie` 객체 사용)
   - `redirect` 파라미터 처리 (로그인 후 원래 페이지로)

2. ✅ `login.jsp`
   - 아이디/비밀번호 입력 폼
   - "아이디 저장" 체크박스
   - `redirect` 파라미터 처리
   - 저장된 아이디 자동 입력 (쿠키에서 읽기)

**파일 위치**: 
- `src/main/java/com/example/servlet/LoginServlet.java`
- `src/main/webapp/jsp/user/login.jsp`

#### 4.3 로그아웃 ✅

**작업 내용**:
- 로그아웃 서블릿 구현

**생성된 파일**:
1. ✅ `LogoutServlet.java`
   - `session.invalidate()` 호출 (`SessionUtil.invalidate()`)
   - "아이디 저장" 쿠키 삭제
   - 홈으로 리다이렉트

**파일 위치**: `src/main/java/com/example/servlet/LogoutServlet.java`

#### 4.4 마이페이지 ✅

**작업 내용**:
- 마이페이지 기본 구조 구현

**생성된 파일**:
1. ✅ `MypageServlet.java`
   - 로그인 체크
   - 사용자 리뷰 목록 조회 (`ReviewDAO.findByUserId()`)
   - 마이페이지로 포워드

2. ✅ `mypage/index.jsp`
   - **평가 요소**: 
     - 세션에서 사용자 정보 가져오기 (`${sessionScope.user}`)
     - JSTL `<c:forEach>`로 내 리뷰 목록 표시
     - JSTL `<c:choose>`, `<c:when>`으로 역할 표시
   - 사용자 정보 표시
   - 내 리뷰 목록 (별점, 제목, 내용, 작성일)
   - 리뷰 수정/삭제 링크

**파일 위치**: 
- `src/main/java/com/example/servlet/MypageServlet.java`
- `src/main/webapp/jsp/mypage/index.jsp`

#### 4.5 비밀번호 유틸리티 ✅

**작업 내용**:
- 비밀번호 해싱 유틸리티 클래스 생성

**생성된 파일**:
1. ✅ `PasswordUtil.java`
   - `hashPassword()`: SHA-256 해싱
   - `verifyPassword()`: 비밀번호 검증
   - `generateSalt()`: 솔트 생성 (향후 BCrypt 사용 시)

**파일 위치**: `src/main/java/com/example/util/PasswordUtil.java`

---

---

### Phase 5: 커뮤니티 코어 - 사케 정보 & 리뷰 ✅

**완료 일시**: 2024년

#### 5.1 사케 목록 페이지 ✅

**작업 내용**:
- 사케 목록 서블릿 및 JSP 페이지 구현

**생성된 파일**:
1. ✅ `SakeListServlet.java`
   - GET `/sake/list`
   - 필터 파라미터 처리 (region, style, keyword)
   - 페이징 파라미터 처리 (page, size)
   - `SakeDAO` 메서드 호출 (findAll, findByRegion, findByStyle, search)
   - 결과를 `request.setAttribute()`로 전달

2. ✅ `sake/list.jsp`
   - **평가 요소**:
     - JSTL `<c:forEach>`로 사케 목록 반복
     - EL `${sake.displayName}` 표현식 사용
     - `<%@ include %>`로 공통 헤더/푸터 포함
   - Bootstrap 카드 형태로 사케 목록 표시
   - 필터 UI (원산지, 스타일, 검색)
   - 페이징 UI (이전/다음, 페이지 번호)

**파일 위치**: 
- `src/main/java/com/example/servlet/SakeListServlet.java`
- `src/main/webapp/jsp/sake/list.jsp`

#### 5.2 사케 상세 페이지 ✅

**작업 내용**:
- 사케 상세 서블릿 및 JSP 페이지 구현

**생성된 파일**:
1. ✅ `SakeDetailServlet.java`
   - GET `/sake/detail?sakeId=`
   - `SakeDAO.findById()` 호출
   - `ReviewDAO.getAverageRating()`, `getReviewCount()` 호출
   - 해당 사케의 리뷰 목록 조회 (`ReviewDAO.findBySakeId()`)
   - 결과를 `request.setAttribute()`로 전달

2. ✅ `sake/detail.jsp`
   - **평가 요소**:
     - 스크립틀릿으로 평점 계산 (`<% %>`)
     - JSTL `<c:forEach>`로 리뷰 목록 표시
     - EL 표현식으로 사케 정보 표시
   - 상단: 사케 기본 정보 + 평균 평점 + 리뷰 수
   - 중간: 맛 프로파일 시각화 (Progress Bar)
   - 하단: 리뷰 리스트 (별점, 제목, 내용, 작성일)

**파일 위치**: 
- `src/main/java/com/example/servlet/SakeDetailServlet.java`
- `src/main/webapp/jsp/sake/detail.jsp`

#### 5.3 리뷰 작성 ✅

**작업 내용**:
- 리뷰 작성 폼 및 서블릿 구현

**생성된 파일**:
1. ✅ `ReviewFormServlet.java`
   - GET `/review/form?sakeId=`: 리뷰 작성 폼 표시
   - 로그인 체크 (AuthFilter로 처리)

2. ✅ `review/form.jsp`
   - **평가 요소**:
     - 폼 태그 (`<form enctype="multipart/form-data">`)
     - 파일 업로드 `<input type="file" multiple>`
     - 클라이언트 측 유효성 검사 (별점 필수, 텍스트 최소 10자)
   - 별점 선택 (1~5, JavaScript로 인터랙티브)
   - 맛 척도 입력 (단맛, 드라이, 산미, 향기, 바디감 - Range Slider)
   - 리뷰 텍스트 입력
   - 사진 업로드 (최대 5장, 미리보기 기능)

3. ✅ `ReviewCreateServlet.java`
   - POST `/review/create`
   - **서버 측 유효성 검사**: 별점 필수, 텍스트 길이
   - **파일 업로드 처리**: 
     - Servlet 3.0+ `Part` API 사용
     - `@MultipartConfig` 어노테이션
     - `/uploads/review/` 디렉토리에 저장
     - 파일명 UUID로 변환 (보안)
     - `uploads` 테이블에 파일 정보 저장
   - `ReviewDAO.insert()` 호출
   - 성공 시 `/sake/detail?sakeId=`로 리다이렉트

**파일 위치**: 
- `src/main/java/com/example/servlet/ReviewFormServlet.java`
- `src/main/java/com/example/servlet/ReviewCreateServlet.java`
- `src/main/webapp/jsp/review/form.jsp`

#### 5.4 리뷰 수정 ✅

**작업 내용**:
- 리뷰 수정 서블릿 구현

**생성된 파일**:
1. ✅ `ReviewUpdateServlet.java`
   - POST `/review/update`
   - 작성자 본인만 수정 가능 체크
   - `ReviewDAO.update()` 호출
   - `review/form.jsp` 재사용 (수정 모드)

**파일 위치**: `src/main/java/com/example/servlet/ReviewUpdateServlet.java`

#### 5.5 리뷰 삭제 ✅

**작업 내용**:
- 리뷰 삭제 서블릿 구현

**생성된 파일**:
1. ✅ `ReviewDeleteServlet.java`
   - GET/POST `/review/delete?reviewId=`
   - 작성자 본인 또는 관리자만 삭제 가능 체크
   - `ReviewDAO.delete()` 호출
   - 관련 파일 삭제 (선택사항)

**파일 위치**: `src/main/java/com/example/servlet/ReviewDeleteServlet.java`

#### 5.6 파일 업로드 유틸리티 ✅

**작업 내용**:
- 파일 업로드 유틸리티 클래스 생성

**생성된 파일**:
1. ✅ `FileUploadUtil.java`
   - `uploadFiles()`: 다중 파일 업로드 처리
   - 파일 크기 제한 (5MB)
   - 파일 개수 제한 (최대 5개)
   - 이미지 확장자 검증
   - UUID 파일명 생성

2. ✅ `UploadDAO.java`
   - `insert()`: 파일 정보 DB 저장
   - `findByReviewId()`: 리뷰별 파일 목록 조회
   - `delete()`: 파일 정보 삭제

**파일 위치**: 
- `src/main/java/com/example/util/FileUploadUtil.java`
- `src/main/java/com/example/dao/UploadDAO.java`

---

### Phase 5.5: 게시판 기능 구현 ✅

**완료 일시**: 2024년

#### 5.5.1 게시판 DB 스키마 추가 ✅

**작업 내용**:
- 게시판 관련 테이블 추가

**생성된 테이블**:
1. ✅ `post` 테이블
   - 게시글 정보 (카테고리, 말머리, 제목, 내용, 조회수, 추천수, 댓글수)
   - 공지글/상단고정 기능
   
2. ✅ `post_comment` 테이블
   - 댓글 정보 (대댓글 지원)
   
3. ✅ `post_like` 테이블
   - 추천 정보 (UNIQUE 제약조건으로 중복 방지)

**파일 위치**: `docker/init.sql`

#### 5.5.2 게시판 Model 클래스 생성 ✅

**생성된 파일**:
1. ✅ `Post.java`: 게시글 모델
2. ✅ `PostComment.java`: 댓글 모델
3. ✅ `PostLike.java`: 추천 모델

**파일 위치**: `src/main/java/com/example/model/`

#### 5.5.3 게시판 DAO 구현 ✅

**생성된 파일**:
1. ✅ `PostDAO.java`
   - `findAll()`: 게시글 목록 조회 (정렬, 카테고리 필터, 페이징)
   - `findById()`: 게시글 상세 조회
   - `insert()`: 게시글 작성
   - `update()`: 게시글 수정
   - `delete()`: 게시글 삭제
   - `incrementViewCount()`: 조회수 증가
   - `incrementLikeCount()` / `decrementLikeCount()`: 추천수 증가/감소
   - `updateCommentCount()`: 댓글 수 업데이트
   - `getTotalCount()`: 전체 게시글 수

2. ✅ `CommentDAO.java`
   - `findByPostId()`: 게시글별 댓글 목록
   - `insert()`: 댓글 작성
   - `update()`: 댓글 수정
   - `delete()`: 댓글 삭제

3. ✅ `PostLikeDAO.java`
   - `exists()`: 추천 여부 확인
   - `insert()`: 추천 추가
   - `delete()`: 추천 취소
   - `getLikeCount()`: 추천 수 조회

**파일 위치**: `src/main/java/com/example/dao/`

#### 5.5.4 게시판 서블릿 구현 ✅

**생성된 파일**:
1. ✅ `BoardListServlet.java`
   - GET `/board/list`
   - 카테고리 필터, 정렬 (최신순/인기글/조회순), 페이징
   - **평가 요소**: JSTL `<c:forEach>`, EL 표현식, 스크립틀릿 날짜 처리

2. ✅ `BoardDetailServlet.java`
   - GET `/board/detail?postId=`
   - 게시글 상세, 조회수 증가, 댓글 목록, 추천 여부 확인

3. ✅ `BoardWriteServlet.java`
   - GET `/board/write`: 작성 폼 표시
   - POST `/board/write`: 게시글 작성 처리
   - 로그인 체크, 서버 측 유효성 검사

4. ✅ `BoardUpdateServlet.java`
   - GET `/board/update?postId=`: 수정 폼 표시
   - POST `/board/update`: 게시글 수정 처리
   - 작성자 본인 또는 관리자만 수정 가능

5. ✅ `BoardDeleteServlet.java`
   - POST `/board/delete`
   - 작성자 본인 또는 관리자만 삭제 가능

6. ✅ `BoardLikeServlet.java`
   - POST `/board/like`
   - AJAX 추천 기능 (JSON 응답)

7. ✅ `CommentCreateServlet.java`
   - POST `/comment/create`
   - 댓글 작성, 댓글 수 자동 업데이트

8. ✅ `CommentDeleteServlet.java`
   - POST `/comment/delete`
   - 댓글 삭제, 댓글 수 자동 업데이트

**파일 위치**: `src/main/java/com/example/servlet/`

#### 5.5.5 게시판 JSP 페이지 구현 (디시인사이드 스타일) ✅

**생성된 파일**:
1. ✅ `board/list.jsp`
   - **평가 요소**:
     - JSTL `<c:forEach>`로 게시글 목록 반복
     - EL 표현식 `${post.title}`, `${post.username}` 등
     - 스크립틀릿으로 `LocalDateTime`을 `Date`로 변환하여 날짜 표시
     - JSTL `<c:choose>`, `<c:when>`, `<c:otherwise>` 조건 분기
   - 탭 메뉴: 전체글, 인기글, 공지, 일반, 질문, 리뷰(술), 주점, 주판점, 양조장, 여행후기
   - 정렬 옵션: 최신순, 인기글, 조회순
   - 테이블 형식: 번호, 말머리, 제목, 글쓴이, 작성일, 조회, 추천
   - 페이징 UI

2. ✅ `board/detail.jsp`
   - **평가 요소**:
     - 스크립틀릿으로 날짜 포맷팅
     - JSTL `<c:forEach>`로 댓글 목록 표시
     - EL 표현식으로 게시글 정보 표시
   - 게시글 상세 정보
   - 추천 버튼 (AJAX)
   - 댓글 작성/삭제 기능

3. ✅ `board/write.jsp`
   - **평가 요소**:
     - 폼 태그 (`<form>`, `<input>`, `<select>`, `<textarea>`)
     - JSTL `<c:if>`로 수정 모드 체크
     - EL 표현식으로 기존 값 표시
   - 카테고리 선택, 말머리 입력, 제목, 내용 입력

**파일 위치**: `src/main/webapp/jsp/board/`

#### 5.5.6 해결한 문제들 ✅

1. **서블릿 등록 문제**
   - **문제**: `init()` 메서드가 서블릿 등록을 방해
   - **해결**: 모든 서블릿에서 `init()` 메서드 제거, `doGet()`/`doPost()`에서만 DAO 생성

2. **JSP 날짜 표시 오류**
   - **문제**: `LocalDateTime`을 `fmt:formatDate`로 직접 처리 불가
   - **해결**: 스크립틀릿으로 `LocalDateTime`을 `java.sql.Timestamp`로 변환 후 `Date`로 변환

3. **서블릿 매핑 중복 오류**
   - **문제**: `@WebServlet` 어노테이션과 `web.xml` 매핑 중복
   - **해결**: `web.xml`에서 서블릿 매핑 제거, `@WebServlet` 어노테이션만 사용

---

## 📝 다음 단계

### Phase 6: 마켓 기능 - 상품 & 장바구니 & 주문 ✅ (완료)

**완료된 작업**:
- ✅ 상품 목록 페이지 (`/product/list`)
  - 지역/스타일 필터, 검색 기능
  - 카드 레이아웃 UI
- ✅ 상품 상세 페이지 (`/product/detail`)
  - 상품 정보 표시, 장바구니 추가 기능
- ✅ 장바구니 기능
  - `CartAddServlet`: 장바구니 추가
  - `CartViewServlet`: 장바구니 조회 (`/cart`)
  - `CartUpdateServlet`: 수량 변경
  - `CartDeleteServlet`: 아이템 삭제
  - `cart/list.jsp`: 장바구니 목록 페이지
- ✅ 주문 기능
  - `OrderFormServlet`: 주문 폼 (`/order/form`)
  - `OrderCreateServlet`: 주문 생성 (트랜잭션 처리, 재고 차감, 장바구니 비우기)
  - `OrderCompleteServlet`: 주문 완료 페이지 (`/order/complete`)
  - `order/form.jsp`, `order/complete.jsp`: 주문 폼 및 완료 페이지

**생성된 파일**:
- `src/main/java/com/example/servlet/ProductListServlet.java`
- `src/main/java/com/example/servlet/ProductDetailServlet.java`
- `src/main/java/com/example/servlet/CartAddServlet.java`
- `src/main/java/com/example/servlet/CartViewServlet.java`
- `src/main/java/com/example/servlet/CartUpdateServlet.java`
- `src/main/java/com/example/servlet/CartDeleteServlet.java`
- `src/main/java/com/example/servlet/OrderFormServlet.java`
- `src/main/java/com/example/servlet/OrderCreateServlet.java`
- `src/main/java/com/example/servlet/OrderCompleteServlet.java`
- `src/main/webapp/jsp/product/list.jsp`
- `src/main/webapp/jsp/product/detail.jsp`
- `src/main/webapp/jsp/cart/list.jsp`
- `src/main/webapp/jsp/order/form.jsp`
- `src/main/webapp/jsp/order/complete.jsp`

**수정된 파일**:
- `src/main/java/com/example/dao/ProductDAO.java`: `findByRegion()`, `findByStyle()`, `search()`, `findByIdWithSakeInfo()` 메서드 추가
- `src/main/java/com/example/model/Order.java`: 배송 정보 필드 추가
- `src/main/java/com/example/dao/OrderDAO.java`: `getTodayOrderCount()` 메서드 추가
- `src/main/webapp/index.jsp`: 메인 화면을 게시판 목록으로 리다이렉트
- `docker/init.sql`: 샘플 상품 데이터 추가

---

### Phase 7: 관리자 페이지 ⏳ (진행 중)

**완료된 작업**:
- ✅ 관리자 대시보드
  - `AdminDashboardServlet`: 통계 데이터 조회 (사케 수, 리뷰 수, 상품 수, 오늘 주문 수)
  - `admin/dashboard.jsp`: 통계 카드 및 인기 사케 TOP 10 표시
- ✅ 사케 관리 (CRUD)
  - `AdminSakeListServlet`: 사케 목록 조회 (검색/필터)
  - `AdminSakeFormServlet`: 사케 등록/수정 폼
  - `AdminSakeCreateServlet`: 사케 등록
  - `AdminSakeUpdateServlet`: 사케 수정
  - `AdminSakeDeleteServlet`: 사케 삭제
  - `admin/sake/list.jsp`, `admin/sake/form.jsp`: 사케 관리 페이지

**생성된 파일**:
- `src/main/java/com/example/servlet/AdminDashboardServlet.java`
- `src/main/java/com/example/servlet/AdminSakeListServlet.java`
- `src/main/java/com/example/servlet/AdminSakeFormServlet.java`
- `src/main/java/com/example/servlet/AdminSakeCreateServlet.java`
- `src/main/java/com/example/servlet/AdminSakeUpdateServlet.java`
- `src/main/java/com/example/servlet/AdminSakeDeleteServlet.java`
- `src/main/webapp/jsp/admin/common/header.jspf`
- `src/main/webapp/jsp/admin/dashboard.jsp`
- `src/main/webapp/jsp/admin/sake/list.jsp`
- `src/main/webapp/jsp/admin/sake/form.jsp`

**수정된 파일**:
- `src/main/java/com/example/dao/SakeDAO.java`: `getTotalCount()`, `findPopularSakes()` 메서드 추가
- `src/main/java/com/example/dao/ReviewDAO.java`: `getTotalCount()` 메서드 추가
- `src/main/java/com/example/dao/ProductDAO.java`: `getTotalCount()` 메서드 추가
- `src/main/java/com/example/dao/OrderDAO.java`: `getTodayOrderCount()` 메서드 추가

**완료된 작업**:
- ✅ 상품 관리 (CRUD)
  - `AdminProductListServlet`: 상품 목록 조회 (검색/필터)
  - `AdminProductFormServlet`: 상품 등록/수정 폼
  - `AdminProductCreateServlet`: 상품 등록
  - `AdminProductUpdateServlet`: 상품 수정
  - `AdminProductDeleteServlet`: 상품 삭제
  - `admin/product/list.jsp`, `admin/product/form.jsp`: 상품 관리 페이지
- ✅ 리뷰 관리
  - `AdminReviewListServlet`: 리뷰 목록 조회 (사케 ID, 사용자 ID, 평점 필터)
  - `AdminReviewDeleteServlet`: 리뷰 삭제
  - `admin/review/list.jsp`: 리뷰 관리 페이지
- ✅ 주문 관리
  - `AdminOrderListServlet`: 주문 목록 조회 (사용자 ID, 주문 상태 필터)
  - `AdminOrderDetailServlet`: 주문 상세 조회
  - `admin/order/list.jsp`, `admin/order/detail.jsp`: 주문 관리 페이지

**생성된 파일**:
- `src/main/java/com/example/servlet/AdminProductListServlet.java`
- `src/main/java/com/example/servlet/AdminProductFormServlet.java`
- `src/main/java/com/example/servlet/AdminProductCreateServlet.java`
- `src/main/java/com/example/servlet/AdminProductUpdateServlet.java`
- `src/main/java/com/example/servlet/AdminProductDeleteServlet.java`
- `src/main/java/com/example/servlet/AdminReviewListServlet.java`
- `src/main/java/com/example/servlet/AdminReviewDeleteServlet.java`
- `src/main/java/com/example/servlet/AdminOrderListServlet.java`
- `src/main/java/com/example/servlet/AdminOrderDetailServlet.java`
- `src/main/webapp/jsp/admin/product/list.jsp`
- `src/main/webapp/jsp/admin/product/form.jsp`
- `src/main/webapp/jsp/admin/review/list.jsp`
- `src/main/webapp/jsp/admin/order/list.jsp`
- `src/main/webapp/jsp/admin/order/detail.jsp`

**수정된 파일**:
- `src/main/java/com/example/dao/ProductDAO.java`: `findAllWithSakeInfo()` 메서드 추가
- `src/main/java/com/example/dao/ReviewDAO.java`: `findAll()` 메서드 추가
- `src/main/java/com/example/dao/OrderDAO.java`: `findAll()` 메서드 추가

---

### Phase 8: 다국어 지원 (i18n) ✅ (완료)

**완료된 작업**:
- ✅ 다국어 리소스 파일 작성
  - `messages_ko.properties`: 한국어 메시지
  - `messages_en.properties`: 영어 메시지
  - `messages_ja.properties`: 일본어 메시지
- ✅ 언어 선택 기능
  - `LanguageServlet`: 언어 변경 처리 (쿠키 + 세션 저장)
  - `LanguageUtil`: 언어 코드 유틸리티 클래스
  - 헤더에 언어 선택 드롭다운 (한국어/English/日本語)
- ✅ 언어 설정 저장
  - 쿠키에 30일간 저장
  - 세션에도 저장
  - 우선순위: 쿠키 > 세션 > 기본값(한국어)

**생성된 파일**:
- `src/main/webapp/WEB-INF/classes/messages_ko.properties`
- `src/main/webapp/WEB-INF/classes/messages_en.properties`
- `src/main/webapp/WEB-INF/classes/messages_ja.properties`
- `src/main/java/com/example/servlet/LanguageServlet.java`
- `src/main/java/com/example/util/LanguageUtil.java`
- `src/main/webapp/jsp/common/i18n_init.jspf`

**수정된 파일**:
- `src/main/webapp/jsp/common/header.jspf`: 언어 선택 드롭다운 개선

---

### Phase 9: ML 서비스 통합 ✅ (완료)

**완료된 작업**:
- ✅ FastAPI ML 서비스 API 엔드포인트
  - `POST /api/v1/recommend`: 사케 추천 (더미 버전)
  - `POST /api/v1/analyze-taste`: 리뷰 텍스트 분석 (키워드 기반 더미)
- ✅ Java 클라이언트 구현
  - `MLClient.java`: ML 서비스 통신 유틸리티
  - JSON 파싱: 정규식 기반 (외부 라이브러리 없이 구현)
  - 헬스 체크 기능 포함
- ✅ 추천 기능 통합
  - `RecommendServlet`: 사용자 기반 추천
  - `recommend/list.jsp`: 추천 사케 목록 페이지
  - 헤더에 "추천" 메뉴 추가
- ✅ 리뷰 텍스트 분석 통합
  - `ReviewCreateServlet`: 리뷰 저장 시 ML 서비스 호출
  - 분석된 맛 태그를 `ml_tags` 컬럼에 저장

**생성된 파일**:
- `src/main/java/com/example/util/MLClient.java`
- `src/main/java/com/example/servlet/RecommendServlet.java`
- `src/main/webapp/jsp/recommend/list.jsp`

**수정된 파일**:
- `ml-service/app/main.py`: 추천 및 맛 분석 API 추가
- `src/main/java/com/example/servlet/ReviewCreateServlet.java`: ML 서비스 통합
- `src/main/webapp/jsp/common/header.jspf`: 추천 메뉴 추가

---

### Phase 10: 최종 테스트 및 배포 ✅ (완료)

**완료된 작업**:
- ✅ 에러 페이지 구현
  - `error/404.jsp`: 404 에러 페이지
  - `error/500.jsp`: 500 에러 페이지
  - `web.xml`에 에러 페이지 설정 완료
- ✅ README.md 작성
  - 프로젝트 개요
  - 실행 방법
  - 주요 기능 설명
  - 문제 해결 가이드

**생성된 파일**:
- `src/main/webapp/jsp/error/404.jsp`
- `src/main/webapp/jsp/error/500.jsp`
- `README.md` (업데이트)

**테스트 결과**:
- 게시판: HTTP 200 ✅
- 상품목록: HTTP 200 ✅
- 추천: HTTP 200 ✅
- ML 서비스: HTTP 200 ✅
- 404 페이지: 정상 작동 ✅

---

## 🔍 테스트 방법

### DB 연결 테스트
1. Docker Compose로 PostgreSQL 실행:
   ```bash
   cd docker
   docker-compose up -d postgres
   ```

2. DB 테이블 확인:
   ```bash
   docker exec -i jsp-hw-postgres psql -U jsp_user -d jsp_hw -c "\dt"
   ```

3. 샘플 데이터 확인:
   ```bash
   docker exec -i jsp-hw-postgres psql -U jsp_user -d jsp_hw -c "SELECT * FROM sake;"
   ```

### 웹 애플리케이션 테스트 (예정)
1. Tomcat 실행
2. `http://localhost:8080/test/db` 접속
3. DB 연결 및 테이블 목록 확인

---

## 📊 진행률

- **Phase 0**: ✅ 100% 완료
- **Phase 1**: ✅ 100% 완료
- **Phase 2**: ✅ 100% 완료
- **Phase 3**: ✅ 100% 완료
- **Phase 4**: ✅ 100% 완료
- **Phase 5**: ✅ 100% 완료 (사케 정보 & 리뷰)
- **Phase 5.5**: ✅ 100% 완료 (게시판 기능)
- **Phase 6**: ✅ 100% 완료 (마켓 기능 - 상품, 장바구니, 주문)
- **Phase 7**: ✅ 100% 완료 (관리자 페이지 - 대시보드, 사케/상품/리뷰/주문 관리)
- **Phase 8**: ✅ 100% 완료 (다국어 지원 - 한국어/영어/일본어)
- **Phase 9**: ✅ 100% 완료 (ML 서비스 통합 - 추천, 맛 분석)
- **Phase 10**: ✅ 100% 완료 (최종 테스트 및 배포 준비)

**전체 진행률**: 100% (모든 기능 구현 완료)

---

## 🐛 알려진 이슈

### 해결된 이슈

#### 1. 글자 깨짐 문제 ✅ (해결됨)
**문제**: 푸터 및 일부 페이지에서 한글이 깨져서 표시됨
**원인**: 
- CharacterEncodingFilter에서 response.setContentType이 JSP의 contentType과 충돌
- Docker 컨테이너의 로케일 설정 부족

**해결 방법**:
- CharacterEncodingFilter에서 response.setContentType 제거 (각 JSP에서 설정)
- Dockerfile에 `ENV LANG=C.UTF-8`, `ENV LC_ALL=C.UTF-8` 추가
- web.xml에 `*.jspf` 패턴도 UTF-8 인코딩 적용

#### 2. 헤더 높이 문제 ✅ (해결됨)
**문제**: 헤더가 너무 높게 표시됨
**원인**: Bootstrap의 `py-3` 클래스로 인한 과도한 패딩

**해결 방법**:
- `py-3`을 `py-2`로 변경
- CSS에서 헤더 컨테이너 패딩 최적화 (0.75rem)
- 헤더 row의 min-height 제거

#### 3. index.jsp 레이아웃 개선 ✅ (완료)
**변경 사항**:
- header.jspf와 footer.jspf를 사용하도록 수정
- Bootstrap 카드 레이아웃으로 개선
- 로그인 상태에 따른 메시지 표시

---

## 📌 참고사항

- DB 연결은 환경 변수를 통해 설정되므로 Docker Compose 환경에서 자동으로 설정됩니다.
- 로컬 개발 시에는 `DataSource.java`의 기본값을 사용합니다.
- 관리자 계정 비밀번호는 BCrypt 해시로 저장되어 있으며, 실제 사용 시에는 평문 비밀번호를 해시하여 저장해야 합니다.

